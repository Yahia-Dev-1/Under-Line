GEMINI_API_KEY=YOUR_GEMINI_API_KEY_HERE

# Notes:
# - Do NOT commit your real API key to the repository.
# - Copy this file to `.env` in the same folder and replace the value with your real key.
# - On Windows PowerShell you can set it for the session with:
#     $env:GEMINI_API_KEY = "your_key_here"
# - Or create a `server\.env` file (remember to add `.env` to .gitignore).